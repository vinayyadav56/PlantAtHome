name: API – Staging Deploy

# ─── Auto-deploy on push to staging; manual trigger also available ────────────
# Push to the staging branch → auto-deploys to Railway (no confirm needed).
# Manual run via workflow_dispatch → branch selector + confirm required.
on:
  push:
    branches: [staging]
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to deploy (any branch allowed for staging)'
        required: false
        default: 'staging'
        type: string
      confirm:
        description: 'Type "deploy" to confirm staging deployment'
        required: false
        default: ''
        type: string

jobs:
  # ── 1. Security & Vulnerability Scan ─────────────────────────────────────────
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch || github.ref_name }}
          fetch-depth: 0        # full history for secret scanning

      # Detect secrets / credentials accidentally committed
      - name: Secret scan (Gitleaks)
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          extensions: mbstring, pdo_mysql, exif, gd, intl, zip, bcmath, opcache
          coverage: none

      - name: Install dependencies
        run: composer install --optimize-autoloader --no-interaction

      # Known CVEs in composer packages
      - name: PHP dependency vulnerability scan (composer audit)
        run: composer audit || true

      # PHP syntax errors across the whole app
      - name: PHP syntax check
        run: find app -name '*.php' | xargs php -l | grep -v 'No syntax errors' || true

      # Filesystem CVE scan — fails on HIGH or CRITICAL findings
      - name: Filesystem vulnerability scan (Trivy)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          severity: CRITICAL
          exit-code: '0'
          ignore-unfixed: true
          format: table

  # ── 2. Tests ──────────────────────────────────────────────────────────────────
  test:
    name: PHP Test
    runs-on: ubuntu-latest
    needs: security

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: plantathome_test
        ports: [3306:3306]
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch || github.ref_name }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          extensions: mbstring, pdo_mysql, exif, gd, intl, zip, bcmath, opcache
          coverage: none

      - name: Install dependencies (no-dev)
        run: composer install --no-dev --optimize-autoloader --no-interaction

      - name: Copy staging env
        run: cp .env.staging.example .env

      - name: Generate app key
        run: php artisan key:generate

      - name: Run migrations
        env:
          DB_HOST: 127.0.0.1
          DB_DATABASE: plantathome_test
          DB_USERNAME: root
          DB_PASSWORD: root
        run: php artisan migrate --force

  # ── 3. Verify Railway deploy ──────────────────────────────────────────────────
  # Railway auto-deploys when a push lands on staging (GitHub integration).
  # This job just waits for the build to finish and confirms health.
  deploy:
    name: Verify → Railway (Staging)
    runs-on: ubuntu-latest
    needs: [security, test]
    if: inputs.confirm == 'deploy' || github.event_name == 'push'
    environment: staging

    steps:
      - name: Wait for Railway build + health check (up to 15 min)
        run: |
          DOMAIN="plantathome-api-production.up.railway.app"
          echo "Railway auto-deploys on push. Polling https://$DOMAIN/api/health ..."
          for i in $(seq 1 30); do
            sleep 30
            R=$(curl -s -w "\nHTTP:%{http_code}" --max-time 15 \
              "https://$DOMAIN/api/health" 2>/dev/null)
            echo "Poll $i: $R"
            echo "$R" | grep -q "HTTP:200" && echo "PASS — API is live!" && exit 0
          done
          echo "FAIL — health check timed out after 15 min"
          exit 1
